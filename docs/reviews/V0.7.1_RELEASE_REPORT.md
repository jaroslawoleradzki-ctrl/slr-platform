# SLR Platform v0.7.1 Release Report

Release date: 2026-09-23 UTC

## Release identity

- Pre-release development SHA: `bf99bb17234c60c185715b19b853c761c6a07a41`
- Version-bump/release-preparation SHAs: `edb2e1d25c462b4dd94025ab2449e6a45e006ecf`
  (`chore(release): prepare v0.7.1`) and `11be89d5e706040e2b99cbd57cc5c454515d0420`
  (`chore(release): synchronize v0.7.1 version metadata`)
- Main SHA before release: `88c63a46131117882fbcd23ebe3c58d7de159887`
- Release-code SHA: `8442be62fc767c578abc980de3e05116d5e9dcf9`
  (merge of `development` into `main` for the v0.7.1 release)
- Final main SHA: recorded after this report is committed; it is intentionally
  one administrative commit ahead of the immutable tag.
- Annotated tag object SHA: `b3878d592e5348ee4d3a4e06b3e9e4cb797a8900`
- Peeled `v0.7.1` target SHA: `8442be62fc767c578abc980de3e05116d5e9dcf9`

The release was integrated by merging `development` into `main` (fast-forward
was impossible: `main` carried the main-only v0.7.0 release report
`88c63a4` on top of the shared base `6530efb`; the merge was automatic with
zero conflicts). The annotated tag was created only after main passed the
release-critical gates and points to the exact release-code merge commit. This
report is administrative documentation committed after tagging; it is not
included in the immutable release tag.

## Version consistency

All authoritative product-version sources report `0.7.1`:

- `VERSION`
- `pyproject.toml` project version
- `uv.lock` editable `slr-platform` package version
- `frontend/package.json`
- `frontend/package-lock.json` root and top-level versions
- `app.core.version.get_app_version()` (reads `VERSION`)

A release-preparation gap (initial prep commit bumped only a subset of these;
`tests/unit/test_versioning.py::test_application_version_is_synchronized`
caught it on main) was fixed by a follow-up version-synchronization commit on
`development`, which was re-merged into `main` before tagging. No application
behavior was changed.

Historical reports, migration history, and release documentation remain
unchanged.

## Verification results

- Development release gates: backend **2,476 passed** with one existing
  Starlette deprecation warning; frontend **340 passed**; TypeScript **PASS**;
  frontend build **PASS**; Ruff **PASS**; mypy **PASS**; `git diff --check`
  **PASS**.
- Main release gates (post-merge, pre-tag): backend **2,476 passed** with one
  existing Starlette deprecation warning; frontend **340 passed**; TypeScript
  **PASS**; frontend build **PASS**; Ruff **PASS**; mypy **PASS**;
  `git diff --check` **PASS**.
- Adversarial v0.7.1 integration suites (WP1/WP2/WP3/final): **39 passed**.
- Tree equivalence: the application tree on main matched the released
  development tree (`11be89d`) except the pre-existing main-only v0.7.0 release
  report; verified via `git diff 11be89d..HEAD --stat` (one file).
- Production-specific record counts are not application assumptions
  (audit: only DOI string fragments in demo fixtures).

## Remote and release verification

- Origin `v0.7.1` tag: annotated object `b3878d592e5348ee4d3a4e06b3e9e4cb797a8900`,
  peeled target `8442be62fc767c578abc980de3e05116d5e9dcf9`.
- HomeLab `v0.7.1` tag: annotated object `b3878d592e5348ee4d3a4e06b3e9e4cb797a8900`,
  peeled target `8442be62fc767c578abc980de3e05116d5e9dcf9`.
- GitHub Release: **PUBLISHED**, tag `v0.7.1`, not draft, not prerelease,
  targeting `8442be6`. Release notes match `docs/releases/v0.7.1.md`.
- Main and development were synchronized to origin and HomeLab before this
  report commit; they will be resynchronized after the report commit.

## Known limitation

Production reconciliation for the affected project dataset is a separate
controlled operation and is NOT executed by upgrading to this release.

## Deployment

Production deployment status: **NOT PERFORMED**. No production Docker,
Portainer, container, or deployment state was modified.
