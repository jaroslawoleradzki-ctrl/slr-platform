# SLR Platform v0.6.9 Release Report

## Pre-release state

- Pre-release development HEAD (post-integration + verification): `ce4170eed3a2580dcdcc217079e34009703e635e`, identical on local, origin, and HomeLab.
- Pre-release main HEAD: `600ede8ba8b057e2b7718ede10938beb7a0a9adb` (release v0.6.8), identical on local, origin, and HomeLab.
- `main` was a direct ancestor of `development` (`development..main` empty); no unexpected main-only commits.
- Known untracked file `v0.6.9-WP1-crossref-retrieval-quality-diagnosis.md` left untouched throughout.

## Version locations updated

Following the exact v0.6.8 `chore(release): prepare` convention (commit `8f621c5`), bumped `0.6.8` -> `0.6.9` in authoritative current-version locations only:

- `VERSION`
- `pyproject.toml` (`project.version`)
- `frontend/package.json` (`version`)
- `frontend/package-lock.json` (top-level + `packages.""` entries; dependency versions untouched)
- `uv.lock` (`slr-platform` package entry only)
- `frontend/tests/Versioning.test.tsx` (`APP_VERSION` expectation)
- `tests/unit/test_versioning.py` (`expected_version`)
- `tests/integration/test_exports_e2e.py` (header + BibTeX/RIS provenance assertions, 5 occurrences)
- `PROJECT_STATUS.md` (Current version + Current release lines)
- `CHANGELOG.md` (new `[0.6.9]` section; historical `[0.6.8]` section untouched)
- `docs/releases/v0.6.9.md` (new release notes file)

Historical references (`docs/releases/v0.6.8.md`, `docs/plans/v0.6.8-*`, review reports, `test_fetch_all_search_resume.py` v0.6.8-checkpoint comment) were NOT rewritten.

## Release notes

- `docs/releases/v0.6.9.md`: "SLR Platform v0.6.9 — Crossref Provenance, Balanced Planning & Uncertainty Experiment", based on integrated changes and the final independent re-review.
- `CHANGELOG.md`: matching `[0.6.9] — 2026-09-22` section.
- Covers: Crossref diagnostic retrieval provenance; corrected Resume accounting/identity; balanced bounded candidate planning; deterministic structured physical-query plan identity; safe incompatible-plan Resume failure before HTTP; canonical JSON plan fingerprinting; literal query delimiter preservation; uncertainty-policy experimental/replay support; corrected NOT positive-group semantics; duplicate DOI accounting across physical queries and Resume; durable checkpoint -> replay -> uncertainty reconciliation; production isolation of the uncertainty experiment; verification/testing status.
- Experimental uncertainty policies are NOT claimed to change production retention behavior.

## Development release gate

On release-prep development `bd458971a9ab8941bf2450dd01922a523f85ce43`:

- `uv run --extra dev pytest -q`: 2389 passed, 1 pre-existing Starlette/httpx warning, exit 0.
- `uv run --extra dev ruff check .`: All checks passed!, exit 0.
- `uv run --extra dev mypy app`: Success: no issues found in 188 source files, exit 0.
- `git diff --check`: clean, exit 0.

Committed as `chore(release): prepare v0.6.9` (`bd45897`), pushed to origin and HomeLab; all development refs identical.

## Development -> main integration

- Local `main` updated safely from `origin/main` (already at `600ede8`, no divergence).
- Normal controlled merge preserving history: `git merge --no-ff development -m "release: v0.6.9"`, following the repo's `release: vX` merge convention (cf. `600ede8` release v0.6.8).
- Merge parents: `600ede8ba8b057e2b7718ede10938beb7a0a9adb` + `bd458971a9ab8941bf2450dd01922a523f85ce43`; zero conflicts.
- Result: `0ec6eb25dc4ca00c411c2cbe7447bb20b370fe40`.

## Main release gate

On real `main` `0ec6eb25dc4ca00c411c2cbe7447bb20b370fe40`:

- `uv run --extra dev pytest -q`: 2389 passed, 1 pre-existing warning, exit 0.
- `uv run --extra dev ruff check .`: All checks passed!, exit 0.
- `uv run --extra dev mypy app`: Success: no issues found in 188 source files, exit 0.
- `git diff --check`: clean.
- Authoritative version source `get_app_version()` reports `0.6.9`.
- Review documents present: `docs/reviews/V0.6.9_FINAL_RE_REVIEW.md`, `docs/reviews/V0.6.9_POST_INTEGRATION_DEVELOPMENT_VERIFICATION.md`, `docs/releases/v0.6.9.md`.

## Repository synchronization

- Development release-prep: local = origin = HomeLab = `bd458971a9ab8941bf2450dd01922a523f85ce43`.
- Main after merge: local = origin = HomeLab = `0ec6eb25dc4ca00c411c2cbe7447bb20b370fe40` (RELEASE MAIN SHA).
- No force push used at any step.

## Tag verification

- `git tag --list v0.6.9` was empty before tagging; existing history untouched.
- Annotated tag `v0.6.9` ("SLR Platform v0.6.9", object `8b3baa434ae4ae709a9e2b79d9596988e45cfb8e`) points exactly to RELEASE MAIN SHA `0ec6eb25dc4ca00c411c2cbe7447bb20b370fe40`.
- Pushed to origin and HomeLab; tag objects byte-identical on both remotes; local peel `v0.6.9^{commit}` = RELEASE MAIN SHA.

## GitHub Release status

- No automated tag-to-release workflow exists in this repository; releases are created manually via GitHub CLI (existing releases up to v0.6.7 confirm the convention; release body = `docs/releases/vX.md`).
- Created via `gh release create v0.6.9 --title "SLR Platform v0.6.9" --notes-file docs/releases/v0.6.9.md`: published, https://github.com/jaroslawoleradzki-ctrl/slr-platform/releases/tag/v0.6.9.

## Preserved review evidence

- `docs/reviews/V0.6.9_FINAL_RE_REVIEW.md` (final independent re-review, all PASS, READY TO MERGE: YES).
- `docs/reviews/V0.6.9_POST_INTEGRATION_DEVELOPMENT_VERIFICATION.md` (post-integration verification, READY FOR RELEASE: YES).
- `docs/reviews/V0.6.9_RELEASE_REPORT.md` (this report).
- Earlier WP2/WP3/WP4 integration and correction reports under `docs/reviews/` preserved.

## Known technical debt

- Whole-payload checkpoint serialization scales quadratically with page count; bounded by existing limits. Retained from the independent review; explicitly NOT a release blocker.

## Deployment status

DEPLOYMENT NOT PERFORMED IN THIS TASK.

No deploy to slr.oleradzki.pl, no production Docker stack changes, no production pull/restart.

## Final release status

- v0.6.9 repository release (code + tag + GitHub Release) complete.
- This report is preserved as a documentation-only follow-up; tag `v0.6.9` remains attached to the verified release-code commit `0ec6eb25dc4ca00c411c2cbe7447bb20b370fe40` and was NOT moved.
