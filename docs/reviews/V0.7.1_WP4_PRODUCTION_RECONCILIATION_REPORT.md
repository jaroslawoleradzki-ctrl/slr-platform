# v0.7.1 WP4: Production Data Reconciliation Foundation

Base SHA: `e25a44f9c837100e5f0e5267388e0f48cef78a00`. Branch/worktree: `feature/v0.7.1-production-reconciliation`, `/home/cloud/dev/slr-platform-v071-production-reconciliation`.

| Dependency | Classification | Handling |
|---|---|---|
| Archive, pre-screening decisions, import history, provenance | REQUIRES ARCHIVE FIRST | verified WP1 archive snapshot; import history retained |
| Normalization, canonical marker, duplicate state | REQUIRES DERIVED-STATE REBUILD | marker reset; run ledger mandates WP2 rebuild |
| Screening, QA, extraction, full text, synthesis | BLOCKED AFTER FORMAL SCREENING / OTHER | explicit screening block plus generic logical-reference scan |
| Finalization/members | BLOCKED AFTER FINALIZATION | explicit membership block |

The command requires database path, project/import, exact unique allow-list, expected count, actor, and backup acknowledgement. It defaults to dry-run. It never uses a status-only delete. It verifies database health, target ownership/import/status, references, finalization/screening, archive completeness, then archives and deletes inside `BEGIN IMMEDIATE`. Pre-commit faults roll back; successful fingerprints replay as deterministic `already-reconciled`.

WP2 integration requirements: consume active rows only, rebuild normalization then deduplication from scratch, and consume `corpus_reconciliation_runs.derived_state_rebuild_required=1` plus WP4 active-count control. WP3 requirements: derive historical retrieval/removal accounting from `import_history`, archive and pre-screening decisions rather than active rows. Neither parallel work package is implemented here.

The disposable 20/12/8 fixture covers dry-run non-mutation, exact archive/delete, history retention, integrity, replay, retained/wrong-import rejection and screening conflict. The same validation covers finalized members, malformed lists, null import IDs, unknown references, archive verification and delete-count rollback. Tests never use production paths. See the runbook for backup/rollback and future production sequence.

Known limitation: WP2/WP3 must be connected in final integration review before production use.

Quality gates: focused WP4 tests passed (`3 passed in 0.10s`). The complete backend suite passed with **2446 passed, 0 failed, 0 skipped, 1 warning in 75.86s**. The warning is a third-party `StarletteDeprecationWarning` from `fastapi/testclient.py` about `httpx` and `starlette.testclient`. Repository Ruff passed (`All checks passed!`); repository mypy passed (`Success: no issues found in 199 source files`). No frontend or TypeScript contract changed. Earlier full-suite attempts stopped at the environment's 30-second command limit; the complete suite was rerun in a persistent session and finished successfully.

PHYSICAL REMOVAL MECHANISM IMPLEMENTED: YES
ARCHIVE REQUIRED BEFORE DELETE: YES
EXACT ALLOW-LIST REQUIRED: YES
DRY-RUN IMPLEMENTED: YES
TRANSACTION SAFETY VERIFIED: YES
FORMAL SCREENING PROTECTED: YES
FINALIZED CORPUS PROTECTED: YES
RETRIEVAL HISTORY SURVIVES PHYSICAL REMOVAL: YES
PROVENANCE SURVIVES PHYSICAL REMOVAL: YES
WP2 CONTRACT DEFINED: YES
WP3 CONTRACT DEFINED: YES
PRODUCTION DATA MODIFIED: NO
PRODUCTION RECONCILIATION EXECUTED: NO
PRODUCTION DEPLOYMENT EXECUTED: NO

EXPECTED FUTURE PRODUCTION ACTIVE CORPUS: 1725

READY FOR WP1+WP2+WP3+WP4 INTEGRATION REVIEW: YES
