# v0.6.9 Final Independent Re-Review

## Exact refs

Fetched `origin --prune --tags` on 2026-09-22.

- `origin/development`: `c31fcba4ba1916dff4e64138099dc1371cb0d6bb`
- `WP2.1` (`feature/v0.6.9-wp2-crossref-provenance`): `21fd1cc8d03aeea8d9437bfb77c2e2fbafd3ed76`
- `WP3.2` (`feature/v0.6.9-wp3-balanced-crossref-planner`): `df3c98ad580dddaf7eb24b23f274ed608c21a349`
- `WP4.2` (`feature/v0.6.9-wp4-crossref-uncertainty-experiment`): `37d35bfa35924d67560c7f1fbaee31a44028a895`

All refs were verified before integration against expected SHAs. Prior review documents (`V0.6.9_WP2_1_WP3_1_WP4_1_FINAL_INTEGRATION_REVIEW.md`, `V0.6.9_WP3_2_RESUME_IDENTITY_CORRECTION_REPORT.md`, `V0.6.9_WP4_2_NOT_SEMANTICS_CORRECTION_REPORT.md`) were read in full and treated strictly as claims to test independently.

---

## Integration topology

A new disposable integration worktree was created at `/tmp/v069-final-re-review` on branch `review/v0.6.9-final-re-review`, branched directly from `origin/development` (`c31fcba4ba1916dff4e64138099dc1371cb0d6bb`).

Logical changes were integrated in the specified order:
1. **WP2.1**:
   - `5664ebc4b9b66d50d6a8dc6690b74947d45c9776` (`feat(crossref): add diagnostic retrieval provenance`)
   - `21fd1cc8d03aeea8d9437bfb77c2e2fbafd3ed76` (`fix(crossref): correct provenance resume identity`)
2. **WP4.2**:
   - `c870e4c3b807d334a4094ea2f4d005f861c3500f` (`feat(search): add uncertainty policy experiment`)
   - `dcc48b16509dd29e96f1b220c237e0afbf26262c` (`fix(search): complete uncertainty integration verification`)
   - `37d35bfa35924d67560c7f1fbaee31a44028a895` (`fix(search): align uncertainty positive group semantics`)
   *(Note: WP4's cherry-picked equivalents of WP2 commits `8e4a9cf` and `137be14` were omitted to prevent duplicate logical patches).*
3. **WP3.2**:
   - `a8618108f8d4ecab15ae1ea91c568cc397e13f31` (`fix(crossref): balance bounded candidate planning`)
   - `e1516badac5e0e1b95c0ef695d2cf5f0df0dd6fd` (`fix(crossref): harden balanced planner resume contract`)
   - `df3c98ad580dddaf7eb24b23f274ed608c21a349` (`fix(crossref): harden resume query identity`)

All eight commits applied cleanly with **zero merge conflicts**.
The exact integration HEAD is:
`b9132a5525f2ceb8c73c396b6adc8c0f8d36099c`

No feature branches were modified. Development was not modified, merged, or pushed.

---

## Previous Sol findings closure

| Previous Sol Finding | Prior Severity | Re-Review Status | Independent Basis |
|---|---|---|---|
| **SOL ORIGINAL MAJOR 3** | MAJOR (Escalated to BLOCKER) | **CLOSED** | Incompatible plan checks execute strictly before HTTP. Legacy cursors and unpinned cursors refuse with `IncompatibleCrossrefPlanError` and 0 HTTP requests. Valid pinned resumes execute exact query and cursor. |
| **SOL ORIGINAL MINOR 7** | MINOR | **CLOSED** | WP4 `_positive_groups` now filters top-level `NOT` exclusions from positive groups and returns empty for pure `NOT`. Canonical positive group count for `AND(Alpha, Gamma, NOT(Beta))` is exactly 2, and for `NOT(Beta)` is exactly 0. |
| **SOL FINGERPRINT BLOCKER** | BLOCKER | **CLOSED** | Plan fingerprint serialization migrated from newline join to canonical JSON array encoding (`json.dumps(..., separators=(',', ':'))`). Ambiguous pair `["a\nb", "c"]` and `["a", "b\nc"]` produces distinct fingerprints (`869e4af18a4b82a0` vs `ba97b7d634f69347`), and cross-application refuses before HTTP. |
| **SOL DELIMITER MAJOR** | MAJOR | **CLOSED** | All `" || "` string splitting for plan reconstruction has been eliminated from `CrossrefProvider`. Structured `candidate_queries` is preserved intact. Query `alpha || beta` issues intact physical request `'"alpha || beta"'`. |
| **SOL INTEGRATED TEST GAP** | MAJOR | **CLOSED** | Complete integrated end-to-end scenarios covering 7x6x4 provenance, 6-query restart with retained/canonical-rejected/constraint-rejected duplicates, SQLite durable checkpoint restoration, and WP4 adapter reconciliation were executed and passed. |

---

## Resume identity adversarial tests

The zero-wrong-Crossref-request invariant was tested against all adversarial edge cases:

1. **Legacy two-element cursor + missing `candidate_queries`**:
   - Attempted resume of `[1, "old-page"]` against a multi-query plan with missing checkpoint `candidate_queries`.
   - Result: `IncompatibleCrossrefPlanError` raised before HTTP.
   - HTTP request count: **0**.
2. **Legacy cursor + malformed `candidate_queries`**:
   - Tested malformations: `[]`, `[123]`, `["valid", ""]`, `["valid", "   "]`, `["valid", None]`.
   - Result: `IncompatibleCrossrefPlanError` raised before HTTP.
   - HTTP request count: **0**.
3. **Unpinned single-query physical cursor**:
   - Attempted resume of a raw cursor `"old-query-page"` without pinned candidate queries.
   - Result: `IncompatibleCrossrefPlanError` raised before HTTP.
   - HTTP request count: **0**.
4. **Valid pinned legacy resume**:
   - Resumed saved plan `["legacy query zero", "legacy query one", "legacy query two"]` at index 1 with cursor `"page-token-abc"`.
   - Result: Exact physical query `"legacy query one"` issued with cursor `"page-token-abc"`.
   - HTTP request count: **1** (exact match).
5. **Cursor index out of bounds**:
   - In-flight cursor at indices 2, 5, 99 on a 1-element plan.
   - Result: `IncompatibleCrossrefPlanError` ("out of bounds") raised before HTTP.
   - HTTP request count: **0**.
6. **Malformed plan metadata in `FetchAllSearchService`**:
   - Checkpoint with in-flight cursor and missing/corrupted `plan_metadata` evaluated by `FetchAllSearchService`.
   - Result: Job marked `status="failed"`, `resumable=False`, `IncompatibleCrossrefPlanError` recorded.
   - HTTP request count: **0**.

---

## Fingerprint adversarial tests

- **Serialization Injection Test**:
  Sol's exact counterexample:
  - Plan 1: `["a\nb", "c"]`
  - Plan 2: `["a", "b\nc"]`
  - Fingerprint 1: `869e4af18a4b82a0`
  - Fingerprint 2: `ba97b7d634f69347`
  - Verified: `fp1 != fp2`.
- **Cross-Application Binding Test**:
  - Encoded cursor using Plan 1 fingerprint and applied to Plan 2.
  - Result: `IncompatibleCrossrefPlanError` raised before HTTP.
  - HTTP request count: **0**.
- **Tampered Fingerprint Test**:
  - Cursor provided with arbitrary mismatched fingerprint `"mismatched_fp_99"`.
  - Result: `IncompatibleCrossrefPlanError` ("does not match active plan fingerprint") raised before HTTP.
  - HTTP request count: **0**.

---

## Literal delimiter test

- Tested legitimate search term containing the delimiter: `alpha || beta`.
- Candidate queries list: `['"alpha || beta"']` (single physical query).
- Request verification: Exactly **1** HTTP request issued to Crossref with parameter:
  `query='"alpha || beta"'`, `cursor="*"`.
- Verified: No splitting on `" || "` occurred. Structured plan identity remained intact.

---

## WP4 NOT semantics

Independently verified in `tests/unit/services/test_uncertainty_experiment.py` and review suite:

1. **`AND(Alpha, Gamma, NOT(Beta))` via `classify_candidate`**:
   - Positive group count: **2** (expected: 2).
   - Evidenced group count: **2**.
   - Canonical status: `MATCH`.
2. **Pure `NOT(Beta)` via `classify_candidate`**:
   - Positive group count: **0** (expected: 0).
   - Evidenced group count: **0**.
   - Canonical status: `MATCH`.
3. **Normal positive `AND(Alpha, Beta, Gamma)`**:
   - Positive group count: **3** (unchanged).
4. **Nested NOT consistency**:
   - `AND(Alpha, OR(Gamma, NOT(Beta)))` -> positive group count: **2**.
5. **Zero-evidence INDETERMINATE**:
   - Candidate with title only and absent abstract -> positive group count: **3**, evidenced: **0**, status: `INDETERMINATE`, classified as `UNCERTAIN_NO_EVIDENCE`.
6. **Execution Ineligibility**:
   - Candidate with `execution_eligible=False` mapped to `EXECUTION_INELIGIBLE` across all policy evaluations; discarded count: **0**.
7. **Replay Adapter**:
   - Adapter behavior consumes WP2 group evidence directly and remains consistent with canonical definitions.

---

## 7x6x4 integration

Executed end-to-end integration test with full Crossref provider pagination:
- **Input Axes**:
  - 7 Lean alternatives
  - 6 Energy alternatives
  - 4 Manufacturing alternatives
  - Total combinations: 168
- **Generated Candidate Plan**: Exactly **6** physical queries.
- **Axis Representation**: `[6, 6, 4]` (satisfies $\min(6, L_j)$ per axis).
- **Physical Execution**:
  - Exactly **6** physical requests issued in deterministic order with `cursor="*"`.
  - Exactly 12 mapped publications (2 per page).
- **WP2 Provenance Verification**:
  - Every mapped publication carries provenance with `physical_query`, `physical_query_index` ($0 \dots 5$), `physical_cursor="*"`, `result_rank` ($0 \dots 1$), and non-null `provider_score`.

---

## Six-query Resume with duplicate classes

Executed full balanced 6-query execution with interruption, checkpoint persistence, and resume across duplicate classes:

1. **Execution Protocol**:
   - 6 physical queries, 2 pages per query (12 physical requests total).
   - Phase 1: Interrupted after 4 requests (Query 0 pages 1-2, Query 1 pages 1-2).
   - Checkpoint: Durable SQLite checkpoint saved with `query_index=2, cursor="*"`, `resumable=True`.
   - Phase 2: Fresh service resumes from SQLite checkpoint and executes remaining 8 requests (Query 2 through Query 5).
2. **Zero Position Replay**:
   - `phase1_requests ∩ phase2_requests = ∅`.
   - Total requests issued: exactly **12** distinct `(query, cursor)` positions.
3. **Duplicate Classes Tested**:
   - **Class A (Retained duplicate)**: DOI `10.9/retained-dup` returned in Q0 p1 and Q1 p1. Matches canonical query, language="en".
     - Outcome: `RETAINED`.
     - Retrieval paths: **2** paths recorded.
     - Canonical count: Counted exactly **once** in `canonical_accepted_count` and `kept_count`.
   - **Class B (Canonical-rejected duplicate)**: DOI `10.9/rejected-canonical-dup` returned in Q0 p2, Q2 p1, Q2 p2. Non-match ceramics paper.
     - Outcome: `REJECTED_CANONICAL`.
     - Retrieval paths: **3** paths recorded across restart boundary.
     - Canonical count: Counted exactly **once** in `canonical_rejected_count`.
   - **Class C (Constraint-rejected duplicate)**: DOI `10.9/rejected-constraint-dup` returned in Q1 p2, Q3 p1, Q3 p2. Canonical match but German (`language="de"`), rejected by English constraint.
     - Outcome: `REJECTED_CONSTRAINTS`.
     - Retrieval paths: **3** paths recorded across restart boundary.
     - Canonical count: Counted exactly **once** in `canonical_accepted_count`, **0** in `kept_count`.
   - **Additional Classes**:
     - Zero-evidence indeterminate (`10.9/indeterminate-zero-evidence`) in Q4 p1, Q4 p2 (2 paths).
     - Unique match (`10.9/unique-match-q5`) in Q5 p1, Q5 p2 (2 paths).
4. **Reconciliation**:
   - Unique candidates: **5**.
   - `fetched_count`: **5**.
   - `kept_count`: **3** (`retained-dup`, `indeterminate-zero-evidence`, `unique-match-q5`).
   - `canonical_accepted_count`: **3** (`retained-dup`, `rejected-constraint-dup`, `unique-match-q5`).
   - `canonical_rejected_count`: **1** (`rejected-canonical-dup`).
   - `canonical_indeterminate_count`: **1** (`indeterminate-zero-evidence`).
   - All retrieval paths preserved in diagnostics: $2 + 3 + 3 + 2 + 2 = 12$ paths.

---

## Checkpoint-to-WP4 reconciliation

Replay dataset generated from the durable SQLite checkpoint of the 6-query run:
- **Durable Checkpoint Restoration**: A fresh `FetchAllSearchService` with no in-memory state loaded the completed job from the SQLite checkpoint repository and called `get_crossref_replay_dataset(job_id)`.
- **Replay Rows**: Exactly **12** path-level rows generated (matching the 12 retrieval paths).
- **WP4 Adapter Execution**: Fed the 12 rows to `adapt_wp2_replay_rows(replay_rows)`.
- **Candidate Cardinality**: Yielded exactly **5** `UncertaintyCandidate` objects. Path multiplicity did **not** inflate population cardinality.
- **Experiment Status Reconciliation**:
  - `match_count`: **3**
  - `non_match_count`: **1**
  - `indeterminate_count`: **1**
- **Policy Reconciliation**:
  - `CURRENT_RECALL_FIRST`:
    - `main_count`: **3** (2 eligible matches + 1 eligible indeterminate)
    - `uncertainty_count`: **0**
    - `rejected_count`: **1** (non-match)
    - `execution_ineligible_count`: **1** (constraint-rejected match)
  - `SEPARATE_ALL_UNCERTAIN`:
    - `main_count`: **2** (2 eligible matches)
    - `uncertainty_count`: **1** (eligible indeterminate)
    - `rejected_count`: **1**
    - `execution_ineligible_count`: **1**
  - `EVIDENCE_TIERED_UNCERTAIN`:
    - `main_count`: **2**
    - `uncertainty_count`: **1**
    - `rejected_count`: **1**
    - `execution_ineligible_count`: **1**

---

## Zero-evidence uncertainty

Verified for the actual replay candidate `10.9/indeterminate-zero-evidence`:
- Missing abstract: `True`
- Evidenced positive groups: `0`
- Canonical status: `INDETERMINATE`
- Execution eligible: `True`
- Classified tier: `UncertaintyTier.UNCERTAIN_NO_EVIDENCE`
- Policy behavior:
  - `CURRENT_RECALL_FIRST` -> `MAIN`
  - `SEPARATE_ALL_UNCERTAIN` -> `UNCERTAINTY`
  - `EVIDENCE_TIERED_UNCERTAIN` -> `UNCERTAINTY`
- Discard check: `discarded_uncertain_count == 0` across all policies; candidate is never silently dropped.

---

## Production isolation

Verified repository-wide:
- No module in production retrieval (`app/providers/`, `app/rendering/`), service orchestrators (`app/services/fetch_all_search.py`, `app/services/live_search.py`), screening, canonical validator, API, or frontend imports `uncertainty_experiment` or `uncertainty_replay_adapter`.
- Only experimental services and tests import the policy code.
- Production retention outcomes (`RETAINED`, `REJECTED_CANONICAL`, `REJECTED_CONSTRAINTS`) are computed independently and are untouched by WP4.

---

## Full integrated gate

1. **Complete Backend Test Suite**:
   - Command: `uv run --extra dev pytest -q`
   - Result: **2,430 passed, 1 pre-existing Starlette/httpx deprecation warning in 75.99s, exit 0**.
2. **Ruff Linter**:
   - Command: `uv run --extra dev ruff check .`
   - Result: **All checks passed! (exit 0)**.
3. **Mypy Type Checker**:
   - Command: `uv run --extra dev mypy app`
   - Result: **Success: no issues found in 188 source files (exit 0)**.
4. **Git Diff Cleanliness**:
   - Command: `git diff --check && git diff --check origin/development HEAD`
   - Result: **Pass, clean whitespace and line endings (exit 0)**.

---

## Findings

All issues identified by Sol are confirmed closed. No regressions or new defects were found.
- Technical debt noted in previous review (whole-payload checkpoint serialization scaling quadratically with page count) remains documented and bounded by existing limits.

---

## Final recommendation

SOL ORIGINAL MAJOR 3: CLOSED
SOL ORIGINAL MINOR 7: CLOSED
SOL FINGERPRINT BLOCKER: CLOSED
SOL DELIMITER MAJOR: CLOSED
SOL INTEGRATED TEST GAP: CLOSED

NEW BLOCKERS: 0
NEW MAJORS: 0
NEW MINORS: 0
NEW NOTES: 0

WP2.1: PASS
WP3.2: PASS
WP4.2: PASS
CROSS-WP INTEGRATION: PASS

ADVERSARIAL TESTS: PASS (18 Sol reproductions + 23 independent re-review tests = 41 passed)
SIX-QUERY DUPLICATE RESUME: PASS (12 requests, 0 replay, 5 candidates, all paths preserved)
CHECKPOINT -> WP4: PASS (12 replay rows -> 5 candidates, exact policy reconciliation)
FULL BACKEND: 2430 passed, 1 warning, 75.99s, exit 0
RUFF: PASS, exit 0
MYPY: PASS, 188 files, exit 0
DIFF CHECK: PASS, exit 0

READY TO MERGE TO DEVELOPMENT: YES
READY TO RELEASE v0.6.9: YES

NO MERGE.
NO PUSH OF REVIEW BRANCH.
NO TAG.
NO RELEASE.
NO DEPLOYMENT.
