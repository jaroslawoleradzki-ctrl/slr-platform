# v0.7.0 post-integration development verification

Date: 2026-09-22 UTC. The reviewed integration was fast-forwarded to local development, then the sole Ruff blocker was resolved by removing five unused imports from three reviewed test files. No production source changed.

## Baseline and integration

- Development before integration: `046e3e7fa1841be84316488e9cc611e4ff51a7ff` (identical to the freshly fetched `origin/development` and `homelab/development`).
- Reviewed integration artifact: `c2481a622ffdad60aa9114fe1687b6a198e027ff`. Local and origin review refs matched it; the committed independent report's final verdict authorizes integration.
- Method: `git merge --ff-only c2481a622ffdad60aa9114fe1687b6a198e027ff` on `development`. The prior development commit is the direct ancestor of the linear review history, so this retains every reviewed commit and its exact tree.
- Resulting local development SHA: `c2481a622ffdad60aa9114fe1687b6a198e027ff`.
- Integrated commits, oldest first: `92ac00836ea5b4cad20e27cfdd188f30cbeb3800` (WP1), `e92e3704ef7d87bcfa68cf30a6c6a6eeea7b96fb` (WP2), `bb5769721250a26c3281f04ffc66b6b41e6e5fe5` (WP3), `0459d131d7bebb9bf1965be790dae0bf5ca905f5` (WP4 contract), `9d9b6ccbd954da374b91b704484b8e09040c2543` (WP1/WP4 reconciliation and adversarial test), `c2481a622ffdad60aa9114fe1687b6a198e027ff` (independent review report).

The initial checkout contained an untracked copy of the independent review report whose Git blob hash matched the committed reviewed version exactly. That copy was preserved at `/tmp/slr-v070-review-report-preserve.pX5YL9/V0.7.0_FINAL_INDEPENDENT_INTEGRATION_REVIEW.md` before fast-forward. Two unrelated untracked v0.6.9 documents were left untouched.

## Reviewed tree preservation and justified difference

Command: `git diff --exit-code c2481a622ffdad60aa9114fe1687b6a198e027ff HEAD -- .gitignore VERSION pyproject.toml uv.lock app/ frontend/ migrations/ tests/`

Result at the fast-forward integration point: exit 0, no output. `git diff --exit-code c2481a622ffdad60aa9114fe1687b6a198e027ff HEAD` also exited 0 with no output. Thus the initially integrated tracked tree exactly matched the reviewed artifact.

The subsequent Ruff cleanup removes only unused imports in `tests/integration/test_pre_screening_review_workflow.py` (`UUID`, `DuplicateDecision`, `DuplicateGroupReviewDecision`), `tests/unit/api/test_pre_screening_api.py` (`UUID`), and `tests/unit/repositories/test_pre_screening_decision_repository.py` (`pytest`). A whole-file reference search found each name only on its import line; existing assertions remained intact, and the adversarial test exercises the duplicate-decision types separately. No assertion, test body, application file, or configuration file changed. The post-integration report is an administrative addition. Running `uv` temporarily updated the local `uv.lock` package version; that generated change was restored.

## Work packages and critical behavior

- WP1 integrated: retained/removed states, reasoned audit, restore, screening exclusion of removed records, and separate PRISMA pre-screening removal accounting passed the focused workflow.
- WP2 integrated: `MATCH`, `NON_MATCH`, and `INDETERMINATE` behavior, missing-abstract recall safety, medical/no-abstract indeterminacy, and researcher pre-screening removal passed.
- WP3 integrated: controlled safety stop, renewed per-execution resume budget, cumulative logical progress, exact query/cursor continuation, repeated resume, DOI deduplication, deterministic provenance/replay, and provider-failure distinction passed the focused and full tests.
- WP4 finalization integrated: explicit persisted finalization, immutable admitted member snapshot, idempotent retry, post-finalization import isolation, duplicate DOI admission protection, and guards on pre-screening mutation passed.

Focused command: `uv run --extra dev pytest -q tests/integration/test_v070_integrated_adversarial.py tests/integration/test_pre_screening_review_workflow.py tests/unit/services/test_corpus_finalization_service.py tests/unit/services/test_crossref_retrieval_quality_wp2.py tests/unit/services/test_fetch_all_search_resume.py` — **54 passed**.

Adversarial command: `uv run --extra dev pytest -q tests/integration/test_v070_integrated_adversarial.py` — **1 passed**. The file contains one parametrization-free A–Y workflow test. The independent report's “4 passed” claim for this file is a reporting/count discrepancy, not a failing test.

Affected-test command: `uv run --extra dev pytest -q tests/integration/test_pre_screening_review_workflow.py tests/unit/api/test_pre_screening_api.py tests/unit/repositories/test_pre_screening_decision_repository.py` — **9 passed**, 1 pre-existing Starlette deprecation warning.

## Full quality gates

| Gate | Actual result |
| --- | --- |
| `uv run --extra dev pytest -q` | **PASS**: 2,429 passed, 1 Starlette deprecation warning, 78.17s after Ruff cleanup. |
| `cd frontend && npm test` | **PASS**: 43 files, 340 tests passed. React test warnings appeared. |
| `cd frontend && npx tsc --noEmit` | **PASS**: exit 0, no diagnostics. |
| `cd frontend && npm run build` | **PASS**: Vite build completed; bundle-size advisory. |
| `uv run --extra dev ruff check .` | **PASS**: all checks passed after removal of the five unused imports. |
| `uv run --extra dev mypy app` | **PASS**: no issues in 196 source files. |
| `git diff --check` | **PASS**: exit 0, no output. |

## Remaining limitations and synchronization

The reviewed `uv.lock` lists the editable package as 0.6.9 while `pyproject.toml` declares 0.7.0, so an ordinary `uv run` rewrites the local lockfile; this generated change was restored to preserve the artifact. This pre-existing version mismatch did not prevent the requested quality gates from passing. WP3's documented limits also remain: legacy checkpoints lack a structured stop reason, and provider-side caps do not prove exhaustive coverage.

All required gates now pass. The Ruff cleanup and this report are ready for a single verification commit and synchronization of `development` to origin and HomeLab. Release preparation may begin once those refs are verified identical. No release, main merge, tag, deployment, branch deletion, or worktree cleanup was performed in this integration.

REVIEWED ARTIFACT PRESERVED: YES
WP1 INTEGRATED: YES
WP2 INTEGRATED: YES
WP3 INTEGRATED: YES
WP4 FINALIZATION INTEGRATED: YES
SCREENING CORPUS IMMUTABILITY: PASS
PRISMA ACCOUNTING: PASS
CROSSREF RECALL SAFETY: PASS
FETCH ALL / RESUME: PASS
ADVERSARIAL INTEGRATION TESTS: PASS
FULL BACKEND SUITE: PASS
FULL FRONTEND SUITE: PASS
STATIC QUALITY GATES: PASS
DEVELOPMENT READY FOR v0.7.0 RELEASE: YES
