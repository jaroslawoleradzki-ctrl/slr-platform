# v0.7.1 production corpus reconciliation (future operation only)

Do not run this until WP1, WP2, WP3 and WP4 pass integration review. Verify deployed v0.7.1, quiesce writes, identify the exact SQLite DB, and run `PRAGMA integrity_check`. Create a fresh timestamped backup without overwriting another backup; record its path, size, SHA-256 and integrity check plus the source integrity result. Generate and independently review a deterministic JSON UUID allow-list for one project/import.

Run `python -m app.tools.reconcile_corpus` without `--execute`, preserve the JSON report, and compare its exact set/count. Only then use the identical arguments with `--execute --backup-verified`. Re-run integrity check; verify all targets are archived and absent from `project_publications`. Rebuild Normalization then Deduplication via integrated WP2, validate historical PRISMA via WP3, verify Screening/finalization unchanged, smoke test, and preserve the report.

Rollback: stop writes; retain evidence; restore only the verified timestamped backup through normal operational controls; validate hash and integrity check; smoke test. Never use broad delete/update SQL.
